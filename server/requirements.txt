Flask>=3.0
Pillow>=10.0
zeroconf>=0.130      # mDNS discovery of screens
requests>=2.31       # pushing URL + refresh to a screen's web_server REST API

# Optional: lossless QOI output (fmt=qoi). Decodes far faster on-device
# than PNG, at roughly PNG-ish size. Skip if you only use JPEG.
# qoi>=0.6
# numpy>=1.24
